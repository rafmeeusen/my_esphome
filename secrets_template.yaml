wifi_ssid: "the ssid" 
wifi_password: "the password"
ota_password: "an OTA password"

### 32-byte base64 encoded string; 
### e.g. $ dd if=/dev/urandom bs=1 count=32  2>/dev/null | base64
api_encryption_key: "an api encryption key"


